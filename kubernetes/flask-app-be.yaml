apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: sfia
  labels:
    app: backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: flask-app
        image: rjagajith/flask-app:1.0
        ports:
        - containerPort: 5001
        env:
        - name: DATABASE_URI
          value: mysql+pymysql://admin:radiatorspoon102!@productiondb.cvuavhfwkpq2.eu-west-1.rds.amazonaws.com/users









